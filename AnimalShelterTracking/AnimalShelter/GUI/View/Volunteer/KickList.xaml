<Window x:Class="AnimalShelter.GUI.View.Volunteer.KickList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AnimalShelter.GUI.View.Volunteer"
        xmlns:theme="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero2"
        mc:Ignorable="d"
        Title="KickList" Height="350" Width="525">
    <Grid Background="#b98e64">
        <ListBox ItemsSource="{Binding NotKickedVolunteers}" SelectedItem="{Binding KickedVolunteer}" 
                 Margin="10" Height="200"/>
        <Button Content="Kick" Command="{Binding AcceptKickCommand}" 
                VerticalAlignment="Bottom" Margin="10" Height="30" Click="AcceptKick_Click"/>
    </Grid>
</Window>
